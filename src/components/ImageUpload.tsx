/**
 * Barrel re-export for backwards compatibility.
 * New code should import from `@/components/upload/ImageUpload` and
 * `@/components/upload/GalleryUpload` directly.
 */
export { ImageUpload } from "./upload/ImageUpload";
export { GalleryUpload } from "./upload/GalleryUpload";
